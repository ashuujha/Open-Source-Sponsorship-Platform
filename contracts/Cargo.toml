[workspace]
resolver = "2"
members = [
    "project-registry",
    "sponsorship-vault",
]

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"
# Optimize for size
strip = true
